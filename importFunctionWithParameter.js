// importFunctionWithParameter.js

// Import the function from the exportFunctionWithParameter module
const squareNumber = require("./exportFunctionWithParameter");

// Call the imported function with a number
const result = squareNumber(5);

// Print the result
console.log(result);
